## general
#### NORMAL
`jk` - `<Esc>`
`;` - `:`

## completions
`<C-b>` - scroll docs down
`<C-f>` - scroll docs up
`<C-Space>` complete
`<C-e>` - abort
`<CR>` - confirm selected
`<Tab>` - select next item
`<S-Tab>` - select prev item

## lsp
#### NORMAL
`K` - show hover docs
`<leader>gd` - go to definition
`<leader>gr` - go to references
`<leader>ca` - code action
`<leader>rn` - rename all

## null-ls
#### NORMAL
`<leader>gf` - auto format file

## mole (_notes_)
#### NORMAL
`<leader>ms` - start notes session in current dir
`<leader>mq` - stop notes session
`<leader>mw` - toggle notes window
#### VISUAL
`<leader>ma` - annotate

## persistence (_sessions_)
#### NORMAL
`<leader>qs` - restore session for current dir
`<leader>ql` - restore last saved session
`<leader>qd` - stop autosave for current session

## bafa
#### NORMAL
`<leader>b` - toggle buffer manager

## camouflage
#### NORMAL
`<leader>ct` - toggle camouflage (mask secrets)
`<leader>cr` - reveal secret under cursor
`<leader>cy` - yank real value of secret

## diffmantic
#### NORMAL
`:Diffmantic` - entry point for smart diffs
`:DiffmanticToggle` - toggle diff view

## neo-tree
#### NORMAL
`<C-n>` - toggle file tree explorer on left
`<leader>bf` - reveal opened buffers in floating window

## telescope
#### NORMAL
`<leader>fg` - find files in current directory

## window-picker
#### NORMAL
`<leader>w` - pick window split to jump into

## pj
#### NORMAL
`<leader>fp` - find projects list
`<leader>fP` - change directory to project

## smart-paste
#### NORMAL / VISUAL
`p` / `P` / `gp` / `gP` - automatic smart paste

## todotxt
#### NORMAL
`<leader>to` - open todo.txt file
`<leader>td` - toggle task status (done/todo)
`<leader>tp` - quick capture new task

## ollama-copilot
#### INSERT
`<Tab>` - accept code suggestion
#### NORMAL
`<leader>os` - manually request suggestion
`<leader>or` - reject current suggestion
